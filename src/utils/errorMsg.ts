export const error = (title: string, message: string, code: number) => {
    return { title, message, code }
}
export const responseBody = (title: string, message: string, code: number) => {
    return { title, message, code }
}